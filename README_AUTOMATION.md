# 📡 نظام البث الآلي المجاني (Data-Over-Radio Automation)

هذا الدليل يشرح كيف يمكنك تشغيل "الطرف الأول" (محطة الإرسال) بشكل آلي بالكامل ومجاني باستخدام خدمات سحابية، بحيث يجلب النظام الأخبار والطقس ويحولها لنغمات ويبثها 24/7.

## 🛠️ المكونات البرمجية المضافة:

1.  **`auto_broadcast_generator.py`**: سكربت بايثون يقوم بجلب البيانات (أخبار، طقس، وقت) وتوليد ملف صوتي (`live_broadcast.wav`) يحتوي على هذه البيانات مشفرة بنغمات FFT.
2.  **`continuous_streamer.py`**: سكربت يقوم بتشغيل المولد بشكل دوري وبث الملف الصوتي باستمرار.

## ☁️ كيف تشغل هذا النظام مجاناً؟

لديك خياران أساسيان للتشغيل المستمر بدون تكلفة:

### الخيار الأول: GitHub Actions (موصى به للأتمتة الدورية)
يمكنك إعداد "Workflow" في GitHub ليقوم بتشغيل السكربت كل ساعة وتحديث ملف صوتي في مستودعك (Repository) أو رفعه لمكان تخزين مجاني.
*   **الميزة**: مجاني تماماً، يعمل في الخلفية، لا يحتاج لجهاز كمبيوتر مفتوح.
*   **العيوب**: ليس "بثاً حياً" لحظياً، بل تحديثات دورية.

### الخيار الثاني: Oracle Cloud / Google Cloud (Free Tier)
يمكنك الحصول على "Virtual Machine" (خادم افتراضي) مجاني مدى الحياة من Oracle Cloud (ARM Ampere instances) أو Google Cloud (e2-micro).
1.  قم بتنصيب Python و FFmpeg على الخادم.
2.  قم بتشغيل `continuous_streamer.py` في الخلفية باستخدام `screen` أو `systemd`.
3.  استخدم خادم **Icecast** مجاني (مثل `giss.tv` أو `free-shoutcast.com`) لبث الصوت عبر الإنترنت.
4.  في موقعك (الطرف الأول)، قم بتوصيل مخرج الصوت من الكمبيوتر بجهاز إرسال FM الصغير.

## 🚀 خطوات التشغيل السريع:

1.  تأكد من وجود المكتبات اللازمة:
    ```bash
    pip install requests numpy scipy
    ```
2.  قم بتشغيل المولد يدوياً للتجربة:
    ```bash
    python3 auto_broadcast_generator.py
    ```
3.  سيتولد ملف `live_broadcast.wav`. هذا هو الملف الذي يجب أن "يسمعه" جهاز إرسال الـ FM الخاص بك.

---
**ملاحظة**: هذا النظام يجعل "الطرف الأول" يعمل كـ "بوت" ذكي يجمع المعلومات ويبثها دون تدخلك الشخصي!
