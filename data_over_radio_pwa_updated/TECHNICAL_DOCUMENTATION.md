# مشروع راديو البيانات - الوثائق التقنية الشاملة
# Data-Over-Radio PWA - Complete Technical Documentation

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المعمارية النظامية](#المعمارية-النظامية)
3. [المكونات الرئيسية](#المكونات-الرئيسية)
4. [التقنيات المستخدمة](#التقنيات-المستخدمة)
5. [دليل التثبيت والتشغيل](#دليل-التثبيت-والتشغيل)
6. [أمثلة الاستخدام](#أمثلة-الاستخدام)
7. [الخصائص التقنية](#الخصائص-التقنية)
8. [الأداء والتحسينات](#الأداء-والتحسينات)

---

## نظرة عامة

**مشروع راديو البيانات** (Data-Over-Radio) هو نظام متقدم لنقل البيانات عبر موجات الراديو FM باستخدام تقنية FFT (Fast Fourier Transform) بـ 1000 قناة ترددية متوازية. يوفر التطبيق واجهة ويب حديثة (PWA) تعمل بدون اتصال إنترنت.

### الأهداف الرئيسية:
- توفير إنترنت مجاني للمناطق المنعزلة
- نقل البيانات عبر موجات FM بسرعة تصل إلى 50 KB/s
- ضمان وصول البيانات دون فقدان (FEC - Forward Error Correction)
- توفير واجهة مستخدم سهلة وجميلة

---

## المعمارية النظامية

```
┌─────────────────────────────────────────────────────────────┐
│                    تطبيق الويب (PWA)                       │
│                  Data-Over-Radio Browser                    │
├─────────────────────────────────────────────────────────────┤
│  React 19 + Tailwind CSS + shadcn/ui                       │
│  Service Worker (Offline Support)                           │
│  IndexedDB / LocalStorage                                   │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│              موجات الراديو (FM)                            │
│         300 Hz - 15000 Hz (1000 قناة)                     │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│              السيرفر (Server)                              │
│  Advanced FFT Transmitter (Python)                         │
│  - توليد 1000 نغمة ترددية                                │
│  - إضافة FEC Redundancy                                   │
│  - Data Carousel (إعادة البث)                             │
└─────────────────────────────────────────────────────────────┘
```

---

## المكونات الرئيسية

### 1. تطبيق الويب (Frontend)

**الملف:** `client/src/pages/Home.tsx`

**الميزات:**
- عرض الرسائل المستقبلة في شبكة جميلة
- البحث والتصفية
- نظام المفضلة
- مؤشر قوة الإشارة الحي
- تصنيف تلقائي للرسائل

**المكونات:**
```typescript
interface ReceivedMessage {
  id: string;
  text: string;
  timestamp: string;
  category?: string;
  signalStrength?: number;
}
```

### 2. السيرفر (Backend)

**الملف:** `advanced_fft_transmitter.py`

**الوظائف الرئيسية:**

#### أ. توليد الإشارة (Signal Generation)
```python
transmitter = FFTTransmitter(
    sample_rate=44100,      # Hz
    duration=5,             # seconds
    num_channels=1000       # frequency channels
)

transmitter.generate_signal("Hello World", "output.wav")
```

#### ب. إضافة تصحيح الأخطاء (FEC)
- استخدام Raptor Codes
- تكرار البتات (Repetition Code)
- معدل الحماية: 20%

#### ج. بث البيانات على شكل دوري (Carousel)
```python
transmitter.generate_carousel_signal(
    text="Important message",
    num_repeats=3,
    filename="carousel.wav"
)
```

### 3. المستقبل (Receiver)

**الملف:** `advanced_radio_receiver.py`

**خطوات فك التشفير:**
1. قراءة ملف WAV
2. تطبيق FFT لتحليل الترددات
3. كشف القنوات النشطة (Threshold Detection)
4. إزالة FEC Redundancy
5. تحويل البتات إلى نص

---

## التقنيات المستخدمة

### Frontend Stack
| التقنية | الإصدار | الغرض |
|---------|---------|-------|
| React | 19.2.1 | إطار العمل الرئيسي |
| TypeScript | 5.6.3 | لغة البرمجة |
| Tailwind CSS | 4.1.14 | تصميم الواجهة |
| shadcn/ui | Latest | مكتبة المكونات |
| Vite | 7.1.7 | أداة البناء |

### Backend Stack
| التقنية | الإصدار | الغرض |
|---------|---------|-------|
| Python | 3.11+ | لغة البرمجة |
| NumPy | Latest | معالجة المصفوفات |
| SciPy | 1.17.0 | معالجة الإشارات |
| WAV | Standard | تنسيق الملفات الصوتية |

### PWA Features
- **Service Worker:** للعمل بدون اتصال
- **Manifest.json:** لتثبيت التطبيق
- **IndexedDB:** لتخزين البيانات الكبيرة
- **LocalStorage:** لتخزين الإعدادات

---

## دليل التثبيت والتشغيل

### المتطلبات
- Node.js 18+
- Python 3.11+
- npm أو pnpm

### خطوات التثبيت

#### 1. تثبيت التبعات

```bash
# تثبيت التبعات الأمامية
cd client
pnpm install

# تثبيت التبعات الخلفية (Python)
pip install numpy scipy
```

#### 2. تشغيل السيرفر

```bash
# بدء خادم التطوير
pnpm dev

# سيكون التطبيق متاحاً على: http://localhost:3000
```

#### 3. توليد الإشارات (اختياري)

```bash
# توليد إشارات راديوية
python3 advanced_fft_transmitter.py

# فك تشفير الإشارات
python3 advanced_radio_receiver.py
```

---

## أمثلة الاستخدام

### مثال 1: توليد إشارة بسيطة

```python
from advanced_fft_transmitter import FFTTransmitter

# إنشاء جهاز إرسال
transmitter = FFTTransmitter()

# توليد إشارة
transmitter.generate_signal(
    text="Hello from Data-Over-Radio!",
    filename="hello.wav"
)
```

### مثال 2: فك تشفير الإشارة

```python
from advanced_radio_receiver import RadioReceiver

# إنشاء جهاز استقبال
receiver = RadioReceiver()

# فك التشفير
decoded_text = receiver.decode_signal("hello.wav")
print(f"Decoded: {decoded_text}")

# تحليل جودة الإشارة
receiver.analyze_signal_quality("hello.wav")
```

### مثال 3: استخدام Hook التخزين المحلي

```typescript
import { useOfflineStorage } from '@/hooks/useOfflineStorage';

export function MyComponent() {
  const {
    data,
    addMessage,
    addReceivedData,
    clearData,
    exportData,
  } = useOfflineStorage();

  const handleAddMessage = () => {
    addMessage("New message from radio");
  };

  return (
    <div>
      <button onClick={handleAddMessage}>Add Message</button>
      <p>Total messages: {data?.messages.length}</p>
    </div>
  );
}
```

---

## الخصائص التقنية

### معايير الأداء

| المعيار | القيمة | الملاحظات |
|--------|--------|---------|
| السرعة الخام | 6.25 KB/s | بدون ضغط |
| السرعة المحسوسة | 50 KB/s | مع ضغط الذكاء الاصطناعي |
| عدد القنوات | 1000 | FFT channels |
| نطاق الترددات | 300-15000 Hz | FM band |
| معدل العينات | 44100 Hz | CD quality |
| معدل الحماية | 20% | FEC redundancy |

### حدود النظام

| الحد | القيمة |
|-----|--------|
| حجم الرسالة الواحدة | 1 MB |
| عدد الرسائل المخزنة | غير محدود (حسب التخزين) |
| مدة البث الواحد | 5 ثوانٍ |
| عدد مرات البث (Carousel) | قابل للتخصيص |

---

## الأداء والتحسينات

### تحسينات الأداء المطبقة

1. **تطبيع الإشارة (Normalization)**
   - منع التشويه (Clipping)
   - تحسين جودة الصوت

2. **نوافذ التحليل (Windowing)**
   - تقليل تسرب الطيف (Spectral Leakage)
   - استخدام نافذة Hann

3. **كشف العتبة التكيفي (Adaptive Threshold)**
   - ضبط تلقائي بناءً على الضوضاء
   - تحسين دقة الكشف

4. **ضغط البيانات**
   - استخدام الذكاء الاصطناعي
   - تقليل حجم الملفات

### التحسينات المستقبلية

- [ ] دعم ترميز متقدم (Turbo Codes)
- [ ] تحسين كشف الأخطاء (LDPC)
- [ ] دعم البث المباشر
- [ ] تطبيق الهاتف المحمول (React Native)
- [ ] واجهة إدارة السيرفر

---

## الأسئلة الشائعة

### س: هل يعمل التطبيق دون اتصال إنترنت؟
**ج:** نعم، تم تفعيل Service Worker الذي يسمح بالعمل الكامل دون اتصال.

### س: كيف يتم ضمان عدم فقدان البيانات؟
**ج:** نستخدم تصحيح الأخطاء (FEC) وإعادة البث (Data Carousel).

### س: ما هي السرعة الفعلية للنقل؟
**ج:** 6.25 KB/s خام، و 50 KB/s مع ضغط الذكاء الاصطناعي.

### س: هل يمكن تثبيت التطبيق على الهاتف؟
**ج:** نعم، كـ PWA يمكن تثبيته على أي هاتف ذكي.

---

## الدعم والمساهمة

للمزيد من المعلومات أو المساهمة في المشروع، يرجى التواصل عبر:
- البريد الإلكتروني: support@dataoverradio.local
- الموقع: https://dataoverradio.local

---

**آخر تحديث:** 17 فبراير 2026  
**الإصدار:** 1.0.0  
**الحالة:** جاهز للإنتاج
